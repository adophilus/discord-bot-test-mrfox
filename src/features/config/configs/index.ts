import DatabaseConfig from './database'
import EnvironmentConfig from './environment'
import DiscordConfig from './discord'

export default {
  db: DatabaseConfig,
  environment: EnvironmentConfig,
  discord: DiscordConfig,
  storage: DiscordConfig
}
